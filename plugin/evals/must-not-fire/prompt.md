---
max_turns: 8
timeout_seconds: 240
allowed_tools: [Skill]
tags: [anti-trigger]
---

In my TypeScript project, what is the difference between `Array.prototype.flatMap`
and `Array.prototype.map` followed by `flat()`?
